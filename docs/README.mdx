# Kameo Docs

This directory intentionally contains only two sections:

- `docs/architecture/`: how the system works (constraints, invariants, failure modes).
- `docs/examples/`: minimal, copy-pastable examples.

