# Architecture

- `type-hash.mdx`: how remote message type routing works.
- `remote-links.mdx`: link lifecycle, socket termination, and signals.
- `exactly_once_ask.md`: session-scoped exactly-once ask semantics (audit-focused).
